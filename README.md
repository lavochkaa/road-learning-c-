# C++ Run Command

Compile and run in one line:

```bash
c++ name1-test.cpp && ./a.out
```

Option with custom output filename:

```bash
c++ name1-test.cpp -o name1-test && ./name1-test
```

Option with installed cp-run.txt

```bash
cprun file_name (without .cpp)
```# road-learning-c-
